# Anagram Gen

A tiny CLI that sorts the letters of a single word.

Usage:

  cargo run -- <word>

Example:

  cargo run -- zebra
  # Output: aberz

Or build and run the binary:

  cargo build --release
  ./target/release/anagram_gen zebra
